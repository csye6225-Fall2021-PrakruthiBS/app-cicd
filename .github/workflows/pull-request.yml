name: pull-request
on:
  pull_request:
    branches: [ main ]
  workflow_dispatch: 
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
      - uses: actions/checkout@v2
